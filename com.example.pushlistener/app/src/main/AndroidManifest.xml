<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The listener posts to a server on the HOST machine, reached through the
         emulator's 10.0.2.2 loopback alias. Cleartext is allowed only for that
         host (see network_security_config.xml) - not globally. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:label="Push Listener"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AppCompat.DayNight">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BIND_NOTIFICATION_LISTENER_SERVICE is a signature|privileged
             permission held by the SYSTEM, which is what lets the OS bind to
             us. The user must still grant access in Settings; there is no way
             to grant it programmatically, and that is by design. -->
        <service
            android:name=".PushListenerService"
            android:exported="true"
            android:label="Push Observatory Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
